name: test

on:
  workflow_dispatch:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - python-version: 3.9
            pep-425-tag: 'cp39-cp39'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout action
      uses: actions/checkout@v2
    - name: Checkout rustfrc
      uses: actions/checkout@v2
      with:
        repository: tmtenbrink/rustfrc
        path: test
    - name: Setup Python environment
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Build manylinux Python wheels
      uses: './'
      with:
        python-install: ${{ matrix.pep-425-tag }}
        python-targets: "python${{ matrix.python-version }}"
        pre-build-command: 'echo Hello world! Testing pre-build command'
        package-path: 'test'
    - uses: actions/upload-artifact@v2
      with:
        name: my-artifact
        path: test/dist/rustfrc-1.0.1-cp39-cp39-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
    - name: Test ls
      run: |
        ls
    - name: Test ls
      run: |
        cd test
        ls
      working-directory: $GITHUB_WORKSPACE
